#include<map>
#include<vector>
#include<iterator>
#include<string>
#include<fstream>
#include<iostream>
#include<algorithm>
using namespace std;
int main()
{
//	vector<int> h;
//	for( int i = 0; i < 10; i++ )
//		h.push_back( i );
//	vector<int>::iterator pos = h.begin();
//	h.erase( pos + 1, pos + 9 );
//	cout << h.size() << endl;
//	for( int i = 0; i < h.size(); i++ )
//		cout << h[i] << endl;
	string str = "";
	str += 'y';
	cout << str << endl;
	return 0;
}

